# LLM(AI) 추가 사용 로드맵 (초안)

이 문서는 지금까지 논의된, **Claude AI를 추가로 쓰면 좋은/써야 하는** 작업들을
정리한 것이다. 각 작업이 AI를 얼마나 자주 쓰는지(=비용이 얼마나 드는지)를 기준으로
분류했다.

여기서 "AI를 쓴다"는 건 공고 텍스트 등을 Claude에 보내서 처리하게 하는 것을
말하고, 보내는 양이 많을수록 비용이 늘어난다.

비용이 언제 발생하는지에 따라 세 가지로 나눴다:
- **이미 쓰는 만큼**: 지금도 돌아가는 AI 매칭 기능 범위 안. 사실상 추가 비용 없음.
- **실험할 때만 (소량)**: 우리가 성능 실험을 돌릴 때만, 소량의 고정 데이터에 대해서만 발생.
- **수집할 때마다 (일부만)**: 매번 공고를 수집할 때 발생하지만, 실패한 일부 공고에만 제한적으로.

참고: 현재 AI 매칭 기능은 opus 모델을 쓰고 있다. 아래 작업들도 sonnet, 많아야
opus 수준이면 충분하다.

---

## 요약 표

| # | 항목 | 언제 비용이 드나 | 추천 모델 |
|---|------|------------------|-----------|
| A | KHIDI 공고 본문에서 마감일 뽑기 | 수집할 때마다 (일부만) | sonnet |
| B | 사이트 구조가 바뀌었을 때 자동 복구 | 수집할 때마다 (일부만) | sonnet |
| C | AI 매칭 정확도 개선 | 대부분 이미 쓰는 만큼 | (기존) opus |
| D | 사용자 정정 데이터 모으기 + 성능 측정 도구 | 실험할 때만 (소량) | (기존) opus |

---

## A. KHIDI 공고 본문에서 마감일 뽑기

**문제**: KHIDI(보건산업진흥원) 공고는 목록에 신청기간이 안 나와서 상당수 공고가
화면에 "날짜 미상"으로 뜬다. 새로 찾은 KHIDI 데이터 피드에는 공고 본문 전체가
들어 있고 거기엔 마감일이 적혀 있지만, 문장이 제각각이라 자동으로 뽑기가 어렵다.

**왜 AI가 필요한가**: 이건 단순히 날짜 형식을 찾는 문제가 아니라, 여러 날짜 중
"이 문장에서 신청 마감일이 무엇인가"를 **문맥으로 이해해야 하는** 문제다. 사람은
공고를 읽으면 바로 아는 것이지만, 규칙 기반 프로그램은 문장의 뜻을 모르기 때문에
못 한다. Claude 같은 AI는 문장의 의미를 이해하므로 이걸 잘한다 — 사람이 읽고
"아, 마감은 7월 20일이네" 하고 판단하는 것과 같은 일을 대신 해줄 수 있다.

**어디에·얼마나**: KHIDI 공고에만, 그리고 본문이 있는 공고에만 적용한다. **제목에
마감일이 적혀 있어서 이미 뽑을 수 있는 공고는 AI를 안 쓰고**, 제목으로 못 뽑은
공고에만 AI를 태우면 사용량을 더 줄일 수 있다.

**안전장치 (제목과 대조)**: 제목에서 뽑은 마감일(정확도 높지만 절반 정도의 공고에만
있음)과 본문에서 AI가 뽑은 마감일을 서로 비교한다.
- 둘이 **같으면** → 확실하다고 보고 확정한다.
- 둘이 **다르면** → AI가 본문에서 엉뚱한 날짜를 집었다는 신호이므로, 본문 값은
  버리고 제목 값(또는 "날짜 미상")을 쓴다. 이렇게 하면 AI가 조용히 틀리는 걸
  우리가 알아챌 수 있다.

**추천 모델**: sonnet.

---

## B. 사이트 구조가 바뀌었을 때 자동 복구

**문제**: 우리가 긁어오는 사이트가 화면 구조를 바꾸면, 그 사이트를 담당하는
수집 코드가 아무것도 못 가져오고 조용히 멈춘다. (지금은 "이상 감지" 표시로
알려주긴 하지만, 저절로 고쳐지진 않는다.)

**왜 AI가 필요한가**: 사이트 화면 내용을 AI에 넘겨서 "공고 목록을 정리해줘"라고
시키면, 정해진 형식을 찾는 기존 방식보다 구조 변경에 훨씬 잘 버틴다.

**어디에·얼마나**: 매번이 아니라, **어떤 사이트가 평소와 달리 갑자기 0건을
가져올 때만** 예비로 작동한다. 실제로 이런 일이 생기는 빈도는 낮다.

**추천 모델**: sonnet.

---

## C. AI 매칭 정확도 개선 (기존 기능 강화)

이미 AI를 쓰고 있는 핵심 기능(공고와 회사 적합도 판정)의 정확도를 높여서,
"확인 필요"로 넘어가는 애매한 공고 수를 줄이는 작업.

- **예시 학습**: 사용자가 실제로 고친 판정 사례를 AI에게 예시로 보여줘서 기준을
  더 분명하게 잡게 한다. → 매번 보내는 양이 조금 늘지만, 캐시 기능으로 완화 가능.
- **판정 기준 다듬기 / 확신도 표시 추가**: AI에게 주는 지시문만 바꾸는 것. → 추가 비용 없음.
- **명백히 안 맞는 공고 미리 걸러내기**: 지역·업력·분야로 봐서 확실히 자격이
  안 되는 공고는 AI에 보내기 전에 코드로 먼저 제외. → **오히려 비용이 줄어든다.**

**전제**: 무엇이 개선인지 측정하려면 D(정정 데이터 + 측정 도구)가 먼저 있어야 한다.

---

## D. 사용자 정정 데이터 모으기 + 성능 측정 도구

C의 개선이 실제로 효과가 있는지 확인하기 위한 밑작업.

- **정정 데이터 모으기**: 사용자가 AI 판정을 뒤집을 때(예: AI는 "부적합"이라 했는데
  사용자가 "적합"으로 고침) 그 정답을 저장해 둔다. → **AI 비용 전혀 없음** (기록만 함).
- **성능 측정 도구**: 우리가 성능 실험을 돌릴 때만, 소량의 정답 데이터(약 30~50건)에
  대해서만 AI를 쓴다. 사용자마다·수집마다 반복되는 비용이 아니다. → **소량·예측 가능.**

---

## 권장 진행 순서

1. **D 먼저** (비용 거의 없음): 사용자 정정 데이터부터 모으기 시작 → 이후 모든 성능
   측정의 밑바탕.
2. **C의 무비용 항목** (판정 기준 다듬기, 사전 걸러내기): 바로 진행 가능.
3. **A** (제목 대조 포함): 좁은 범위에서 AI 추출을 처음 도입해, sonnet 추출 + 제목
   대조 방식이 실제로 잘 되는지 검증.
4. **B**: A에서 검증된 방식을 사이트 구조 변경 대응으로 확장.
